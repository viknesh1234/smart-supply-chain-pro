# App Name
spring.application.name=inventory-system

# Server
server.port=8080

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/inventorydb
spring.datasource.username=postgres
spring.datasource.password=MV6381#
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Disable Spring Security (optional, for development)
spring.security.enabled=false

# CORS Configuration (Allow React Frontend)
spring.web.cors.allowed-origins=http://localhost:5173

# =========================
# ? Spring Mail Config (Gmail SMTP)
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vikneshr2710@gmail.com
spring.mail.password=ulyxgcnhxbjwduel
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
